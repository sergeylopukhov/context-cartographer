# Project instructions

This repository holds the reading planner service. Keep changes inside the
`planner/` package and update `docs/architecture.md` when a module boundary
changes.

Run the checks before every change: `python3 -m unittest discover -s tests`.
