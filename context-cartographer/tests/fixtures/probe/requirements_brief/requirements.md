# Requirements for the reading planner

A volunteer opens the planner, picks a date and prints one page with the
readings for that day. The page fits a single sheet and works on the parish
laptop without a network connection.

Out of scope for the first version: accounts, editing, reminders and any
integration with an external calendar service.
